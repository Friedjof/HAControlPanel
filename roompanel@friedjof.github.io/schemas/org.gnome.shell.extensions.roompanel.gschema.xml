<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.roompanel"
          path="/org/gnome/shell/extensions/roompanel/">

    <!-- Home Assistant Connection -->
    <key name="ha-url" type="s">
      <default>""</default>
      <summary>Home Assistant URL</summary>
      <description>Base URL of the Home Assistant instance, e.g. http://homeassistant.local:8123</description>
    </key>
    <key name="ha-token" type="s">
      <default>""</default>
      <summary>Home Assistant Long-Lived Access Token</summary>
      <description>Long-lived access token for authenticating with Home Assistant</description>
    </key>
    <key name="ha-verify-ssl" type="b">
      <default>true</default>
      <summary>Verify SSL Certificate</summary>
      <description>Whether to verify the SSL certificate of the Home Assistant instance</description>
    </key>

    <!-- Color Picker -->
    <key name="color-entities" type="as">
      <default>@as []</default>
      <summary>Color Picker Entity IDs</summary>
      <description>Home Assistant entities to control with the color picker (up to 4)</description>
    </key>
    <key name="color-selected" type="as">
      <default>@as []</default>
      <summary>Color Picker Selected Entities</summary>
      <description>Currently selected entities for color control (empty = all selected)</description>
    </key>
    <key name="color-service" type="s">
      <default>"light.turn_on"</default>
      <summary>Color Picker Service</summary>
      <description>Service to call for color changes, e.g. light.turn_on</description>
    </key>
    <key name="color-attribute" type="s">
      <default>"rgb_color"</default>
      <summary>Color Picker Service Attribute</summary>
      <description>Service data attribute for color, e.g. rgb_color</description>
    </key>

    <!-- Slider -->
    <key name="slider-entities-config" type="s">
      <default>"[]"</default>
      <summary>Slider Entities Configuration</summary>
      <description>JSON array of slider entity configs: [{entity_id, service, attribute, min, max}]</description>
    </key>
    <key name="slider-selected" type="as">
      <default>@as []</default>
      <summary>Slider Selected Entities</summary>
      <description>Currently selected entities for slider control (empty = all selected)</description>
    </key>

    <!-- Action Buttons -->
    <key name="button-count" type="i">
      <default>2</default>
      <range min="0" max="10"/>
      <summary>Number of Action Buttons</summary>
      <description>How many action buttons to show in the panel menu</description>
    </key>
    <key name="buttons-config" type="s">
      <default>"[]"</default>
      <summary>Action Buttons Configuration</summary>
      <description>JSON array with button configurations (label, icon, entity_id, domain, service, service_data)</description>
    </key>

    <!-- Sensor Widgets -->
    <key name="sensor-widgets-config" type="s">
      <default>"[]"</default>
      <summary>Sensor Widget Configurations</summary>
      <description>JSON array of sensor widget configs: [{entity_id, widget_type, attribute, unit_override, icon, min, max, severity, span}]</description>
    </key>

    <!-- YAML Backup -->
    <key name="auto-yaml-backup" type="b">
      <default>false</default>
      <summary>Auto YAML Backup</summary>
      <description>Automatically sync settings to a YAML file on every change</description>
    </key>
    <key name="yaml-backup-path" type="s">
      <default>""</default>
      <summary>YAML Backup File Path</summary>
      <description>Path where the YAML backup file is written</description>
    </key>

  </schema>
</schemalist>
